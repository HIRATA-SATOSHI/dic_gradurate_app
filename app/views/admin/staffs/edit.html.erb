<div class="alert alert-warning" role="alert">
  管理者 スタッフ情報編集
</div>


<br>

<% if @staff.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@staff.errors.count, "error") %> prohibited this <%= @staff.name %> from being saved:</h2>
    <ul>
    <% @staff.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
<% end %>

<%= render 'form', staff: @staff %>
<br>
<%= link_to '戻る', admin_staffs_path %>

